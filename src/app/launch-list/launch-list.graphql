query pastLaunchesList($limit: Int!){
    launchesPast(limit: $limit){
    id
    links {
      flickr_images
      mission_patch_small
    }
    mission_name
    rocket {
      rocket_name
    }
    launch_date_utc
  }
}